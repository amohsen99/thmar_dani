<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Record rule: portal users can only access their own leaves -->
    <record id="hr_leave_portal_rule" model="ir.rule">
        <field name="name">Portal: Own Leaves Only</field>
        <field name="model_id" ref="hr_holidays.model_hr_leave"/>
        <field name="domain_force">[('employee_id.user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

    <!-- Record rule: portal users can read leave types -->
    <record id="hr_leave_type_portal_rule" model="ir.rule">
        <field name="name">Portal: Read Leave Types</field>
        <field name="model_id" ref="hr_holidays.model_hr_leave_type"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <!-- Record rule: portal users can read their own employee record -->
    <record id="hr_employee_portal_rule" model="ir.rule">
        <field name="name">Portal: Own Employee Only</field>
        <field name="model_id" ref="hr.model_hr_employee"/>
        <field name="domain_force">[('user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>

</odoo>
