<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!-- Payment Approval Group -->
        <record id="group_payment_approval" model="res.groups">
            <field name="name">Payment Approver</field>
            <field name="comment">Users in this group can approve payments before confirmation</field>
            <field name="implied_ids" eval="[(4, ref('account.group_account_invoice'))]"/>
        </record>
    </data>
</odoo>

